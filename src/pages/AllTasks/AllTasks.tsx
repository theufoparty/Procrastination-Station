import React from 'react';
import styled from 'styled-components';

const TaskListContainer = styled.div`
  display: flex;
`;

const AllTasks: React.FC = () => {
  return <TaskListContainer>hej</TaskListContainer>;
};

export default AllTasks;
